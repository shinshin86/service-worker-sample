{
  "scripts": {
    "dev": "browser-sync start --server --files *.html --no-open",
    "fmt": "prettier --write **/*.{js,json,md,html}"
  },
  "prettier": {
    "singleQuote": true
  },
  "devDependencies": {
    "browser-sync": "^2.27.9",
    "prettier": "^2.6.2"
  }
}
